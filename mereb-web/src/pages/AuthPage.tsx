// import {Auth, useAppDispatch, useAppSelector} from 'shared-components';

// import {login, register} from 'shared-components';
import Auth from "../components/Auth.tsx";

const AuthPage = () => {
    return <Auth/>;
};

export default AuthPage;
